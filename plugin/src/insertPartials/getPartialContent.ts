/**
 * Returns the content of a given path as a string.
 * 
 * @throws SyntaxError if the file tries to import any other partials
 */
export default function getPartialContent(path: string) : string {
    return "";
}